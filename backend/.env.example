# App Configuration
APP_PORT=3000

# CORS Allowed Origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_postgres_password_here
DB_DATABASE=kuph_db
DB_SCHEMA=kuph

# Keycloak Identity Provider
KEYCLOAK_AUTH_SERVER_URL=http://localhost:8080
KEYCLOAK_REALM=kuph
KEYCLOAK_CLIENT_ID=kuph-backend
KEYCLOAK_CLIENT_SECRET=your_keycloak_client_secret_sample_token
KEYCLOAK_FRONTEND_CLIENT_ID=kuph-frontend

# AWS / LocalStack Configuration
AWS_ENDPOINT=http://localhost:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
SES_FROM_EMAIL=noreply@kuph.app

# AWS S3 (Storage)
S3_BUCKET_NAME=kuph-assets-bucket
